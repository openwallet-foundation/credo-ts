---
"@credo-ts/core": patch
---

feat: support `locale` for SD-JWT VC Type Metadata in addition to `lang`. Note that both variablaes are optional now as we don't know if the old or new metadata structure is used
