---
"@credo-ts/core": patch
---

Add a standalone `JsonLdModule` and shared `JsonLdModuleConfig` for configuring document loaders across JSON-LD consumers. `W3cCredentialsModule` continues to support its existing `documentLoader` configuration API.
